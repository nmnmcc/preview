import { Card } from "./Card";

export const App = () => (
  <Card
    action="Mark ready"
    body="This component runs in a normal Vite application and in a Preview Sandbox."
    confirmedAction="Ready"
    eyebrow="React + Vite"
    heading="One component, two mounts."
  />
);
