/**
 * Describes one PNG file made from a preview target.
 */
export type { GeneratedArtifact } from "./internal/generation";

/**
 * Describes one preview target that could not be generated.
 */
export type { GenerationFailure } from "./internal/generation";

/**
 * Defines the artifacts and failures from one generation run.
 */
export { GenerationSummary } from "./internal/generation";
